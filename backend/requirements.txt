fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
websockets>=13.0
httpx>=0.27
pydantic>=2.8
pydantic-settings>=2.4
apscheduler>=3.10,<4
fastapi-users[sqlalchemy]>=13,<15
httpx-oauth>=0.14
aiosmtplib>=3.0
duckdb>=1.1
# XLSX risk-list downloads (NZ MFAT register, Australia DFAT list, ICCAT IUU)
openpyxl>=3.1
pytz>=2024.1
# rate limiting primitive (same lib slowapi/flask-limiter build on). Memory
# backend by default; point RATELIMIT_STORAGE_URI at Redis/Valkey to share the
# counters across web replicas. Add limits[async-redis] when doing so.
limits>=3.13
# SAR ship detection: chip fetch (tifffile decodes the float32 TIFF - Pillow
# garbles its float predictor), CFAR thresholding (numpy), chips on MinIO/Wasabi (boto3)
numpy>=1.26
tifffile>=2024.2
boto3>=1.34
onnxruntime>=1.17
pillow>=10.0  # decode RGB optical chips for the optical ship detector
